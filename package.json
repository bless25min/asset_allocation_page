{
  "name": "asset-allocation-page",
  "version": "1.0.0",
  "description": "Asset Allocation Simulator",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hono": "^4.11.7"
  }
}
